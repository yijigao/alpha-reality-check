# Provenance

The project was generalized from an internal forward-evidence workflow used to
review quantitative strategy experiments. No proprietary strategy logic,
private market data, account information, or execution credentials are part of
the open-source repository.

All committed example datasets are generated with a fixed random seed and use
neutral labels such as `ASSET_A`, `ASSET_B`, and `ASSET_C`.
